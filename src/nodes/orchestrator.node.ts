import type { PipelineNode } from "./types.js";

export const orchestratorNode: PipelineNode = async () => {
  throw new Error(
    "[croupier] orchestrator.node: la generación LLM (DeepSeek) aún no está implementada."
  );
};
