import type { PipelineNode } from "./types.js";

export const implementerNode: PipelineNode = async () => {
  throw new Error(
    "[croupier] implementer.node: la generación LLM (DeepSeek) de código aún no está implementada."
  );
};
